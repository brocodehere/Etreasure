# Etreasure Admin SPA

React + Vite + Tailwind-based admin panel for managing products, categories, media, and orders.

## Quick start

```bash
cd admin
npm install
npm run dev
```

The app expects the Go backend to run on `http://localhost:8080` and exposes a single global stylesheet at `styles/global.css`.
