# Test Environment Variables for SMTP

# Add these to your .env file or set them in your environment
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=digit9926@gmail.com
SMTP_PASSWORD=oejnevgsdpukwqmo

# For testing without actual email, you can leave SMTP_EMAIL and SMTP_PASSWORD empty
# The system will show warnings but allow testing the flow
